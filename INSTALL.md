# 安装说明

把 `patch-to-powerup` 文件夹放到 Codex skills 目录即可。

常见位置：

```bash
~/.codex/skills/patch-to-powerup
```

这个 skill 适合和以下 skill 配合使用：

- `capability-frontloading`: 当能力存在但没有进入主链路。
- `single-contract-evolution`: 当补丁、旁路、fallback、validator 开始堆叠。
- `good-start-skill`: 当新项目启动时，需要默认带上防补丁化的合同和工程模式。
